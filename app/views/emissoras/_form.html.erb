<%= form_for(@emissora) do |f| %>
  <% if @emissora.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emissora.errors.count, "error") %> prohibited this emissora from being saved:</h2>

      <ul>
      <% @emissora.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :canal %><br />
    <%= f.text_field :canal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
